%YAML:1.0

# Camera calibration and distortion parameters (OpenCV) 
Camera.fx: 900.0   # Focal length in pixels (approx for phone)
Camera.fy: 900.0   # Focal length in pixels
Camera.cx: 960.0   # Principal point (half of 1920)
Camera.cy: 540.0   # Principal point (half of 1080)

Camera.k1: 0.0     # Radial distortion (phones usually have little distortion)
Camera.k2: 0.0
Camera.p1: 0.0     # Tangential distortion
Camera.p2: 0.0
Camera.k3: 0.0

Camera.width: 1920
Camera.height: 1080

# Camera frames per second 
Camera.fps: 30.0

# Color order of the images (0: BGR, 1: RGB. It is ignored if images are grayscale)
Camera.RGB: 1

# Close/Far threshold. Baseline times.
ThDepth: 40.0

# ORB Parameters
# Max ORB features per image
ORBextractor.nFeatures: 1000

# Scale factor between levels in the scale pyramid 
ORBextractor.scaleFactor: 1.2

# Number of levels in the scale pyramid  
ORBextractor.nLevels: 8

# Fast threshold
# Image is divided in a grid. At each cell FAST are extracted imposing a minimum response.
# Firstly we impose iniThFAST. If no corners are detected we impose a lower value minThFAST
# You can lower these values if your images have low contrast 
ORBextractor.iniThFAST: 20
ORBextractor.minThFAST: 7

# Viewer Parameters
Viewer.KeyFrameSize: 0.05
Viewer.KeyFrameLineWidth: 1
Viewer.GraphLineWidth: 0.9
Viewer.PointSize: 2
Viewer.CameraSize: 0.08
Viewer.CameraLineWidth: 3
Viewer.ViewpointX: 0
Viewer.ViewpointY: -0.7
Viewer.ViewpointZ: -1.8
Viewer.ViewpointF: 500
